# LivePage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把课中实时录音页从 SessionPage stub 升级为基于 NotesPage 代码 fork 的 LivePage，支持 WebSocket 流式 ASR 字幕、用户翻页驱动页码匹配、手动触发 AI 解释，并在录音结束后无缝衔接现有 ProcessingPage → NotesPage 流程。

**Architecture:** LivePage 直接 fork NotesPage 代码，移除静态 API 拉取，替换为 WebSocket 流式 ASR；后端新增 `live.py` 路由处理 WebSocket 转发和 SSE 解释生成；前端入口通过 LobbyPage → UploadPage → LivePage 的路径进入。

**Tech Stack:** React + TypeScript（前端）、FastAPI WebSocket + SSE（后端）、阿里云流式 ASR NLS WebSocket、Anthropic Claude API（SSE 解释生成）

---

## 文件结构

| 操作 | 文件 | 职责 |
|---|---|---|
| 新建 | `frontend/src/pages/LivePage.tsx` | 课中录音主页面（fork 自 NotesPage） |
| 删除 | `frontend/src/pages/SessionPage.tsx` | 废弃，不再使用 |
| 修改 | `frontend/src/App.tsx` | 路由：/session → /live，引入 LivePage |
| 修改 | `frontend/src/pages/LobbyPage.tsx` | 绿色按钮改跳 /upload，删除 NewClassModal |
| 修改 | `frontend/src/pages/UploadPage.tsx` | 上传区上方加 Live 入口卡片 |
| 修改 | `frontend/src/lib/i18n.ts` | 新增 live_* key（中英双语） |
| 新建 | `backend/routers/live.py` | WebSocket /ws/live-asr + POST /api/live/explain |
| 修改 | `backend/main.py` | 挂载 live router |

---

## Task 1: i18n — 新增 live_* key

**Files:**
- Modify: `frontend/src/lib/i18n.ts`

- [ ] **Step 1: 在 i18n.ts 的 en 对象末尾（SessionPage key 之后）加入 live_* 英文 key**

找到 `session_submit_error` 那行之后，加入：

```ts
    // LivePage
    live_card_title: 'Live Class',
    live_card_desc: 'Real-time recording · Live transcript · In-class AI explanation',
    live_card_cta: 'Start Live Class',
    live_subtitle_placeholder: 'Subtitles will appear here once recording starts',
    live_explain_empty: 'Recording in progress. Click below for AI explanation',
    live_explain_btn: 'Explain This Page',
    live_explain_refresh: 'Re-explain',
    live_recording_required: 'Please start recording first',
    live_recording_label: 'LIVE RECORDING',
    live_stopped_label: 'STOPPED',
    live_idle_label: 'NOT STARTED',
```

- [ ] **Step 2: 在 zh 对象对应位置加入中文 key**

找到 zh 对象里 SessionPage 注释块之后加入：

```ts
    // LivePage
    live_card_title: '直播课堂',
    live_card_desc: '实时录音 · 实时字幕 · 课中 AI 解释',
    live_card_cta: '进入直播课堂',
    live_subtitle_placeholder: '开始录音后，字幕将在这里实时显示',
    live_explain_empty: '录音中，点击下方按钮获取 AI 解释',
    live_explain_btn: '解释这页',
    live_explain_refresh: '重新解释',
    live_recording_required: '请先开始录音',
    live_recording_label: '录音中',
    live_stopped_label: '已停止',
    live_idle_label: '未开始',
```

- [ ] **Step 3: 确认 i18n.ts 两个 key 数量一致**

在 i18n.ts 文件中搜索 `live_`，en 和 zh 各应出现 11 个 live_ key，数量相同。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/lib/i18n.ts
git commit -m "feat: add live_* i18n keys (zh + en)"
```

---

## Task 2: 路由 — App.tsx 切换到 LivePage

**Files:**
- Modify: `frontend/src/App.tsx`
- Delete: `frontend/src/pages/SessionPage.tsx`

- [ ] **Step 1: 新建空的 LivePage.tsx 占位文件**

在 `frontend/src/pages/LivePage.tsx` 创建最简占位，使 TypeScript 不报错：

```tsx
export default function LivePage() {
  return <div>LivePage placeholder</div>
}
```

- [ ] **Step 2: 修改 App.tsx**

将：
```tsx
import SessionPage from './pages/SessionPage'
```
替换为：
```tsx
import LivePage from './pages/LivePage'
```

将路由部分：
```tsx
<Route path="/session"      element={<SessionPage />} />
<Route path="/session/live" element={<Navigate to="/session" replace />} />
```
替换为：
```tsx
<Route path="/live" element={<LivePage />} />
```

- [ ] **Step 3: 确认前端编译无报错**

```bash
cd frontend && npx tsc --noEmit
```

预期：0 errors（SessionPage 不再被引用，无孤立 import）

- [ ] **Step 4: 删除 SessionPage.tsx**

```bash
git rm frontend/src/pages/SessionPage.tsx
```

- [ ] **Step 5: Commit**

```bash
git add frontend/src/App.tsx frontend/src/pages/LivePage.tsx
git commit -m "feat: replace SessionPage with LivePage route /live"
```

---

## Task 3: LobbyPage — 绿色按钮改跳 /upload，删除 NewClassModal

**Files:**
- Modify: `frontend/src/pages/LobbyPage.tsx`

- [ ] **Step 1: 在 LobbyPage.tsx 顶部确认有 useNavigate**

文件顶部应已有：
```tsx
import { useNavigate } from 'react-router-dom'
```
若无则加入。同时确认组件内有：
```tsx
const navigate = useNavigate()
```

- [ ] **Step 2: 侧边栏绿色按钮（约 L785）改为跳转**

将：
```tsx
onClick={() => setShowModal(true)}
```
改为：
```tsx
onClick={() => navigate('/upload')}
```

- [ ] **Step 3: 空状态绿色按钮（约 L904）同样改为跳转**

将：
```tsx
onClick={() => setShowModal(true)}
```
改为：
```tsx
onClick={() => navigate('/upload')}
```

- [ ] **Step 4: 删除 showModal state 及 NewClassModal 渲染**

删除：
```tsx
const [showModal, setShowModal] = useState(false)
```

删除文件底部的：
```tsx
{showModal && <NewClassModal onClose={() => setShowModal(false)} onUploaded={(sid) => { handleUploaded(sid); setShowModal(false) }} />}
```

- [ ] **Step 5: 删除 NewClassModal 组件定义**

删除从 `function NewClassModal(` 开始到其对应 `}` 结束的整个组件函数（约 L405–L630，视实际行数而定）。

同时删除 NewClassModal 内部用到但其他地方没用的 import（如果有）。

- [ ] **Step 6: 确认编译无报错**

```bash
cd frontend && npx tsc --noEmit
```

预期：0 errors

- [ ] **Step 7: Commit**

```bash
git add frontend/src/pages/LobbyPage.tsx
git commit -m "feat: lobby green button navigates to /upload, remove NewClassModal"
```

---

## Task 4: UploadPage — 新增 Live 入口卡片

**Files:**
- Modify: `frontend/src/pages/UploadPage.tsx`

- [ ] **Step 1: 确认 UploadPage 顶部有 useNavigate 和 useTranslation**

文件顶部应有：
```tsx
import { useNavigate } from 'react-router-dom'
import { useTranslation } from '../context/TranslationContext'
```
组件内有：
```tsx
const navigate = useNavigate()
const { t } = useTranslation()
```
若无则加入。

- [ ] **Step 2: 在两个上传区的 div 之前插入 Live 入口卡片**

找到 `{/* Two side-by-side drop zones */}` 这行注释（约 L310），在其**上方**插入：

```tsx
{/* Live 入口卡片 */}
<div
  style={{
    border: '1.5px solid #798C00',
    borderRadius: '12px',
    padding: '24px 32px',
    marginBottom: '24px',
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: 'rgba(121,140,0,0.04)',
  }}
>
  <div>
    <div style={{ fontWeight: 700, fontSize: '18px', color: '#2F3331', marginBottom: '4px' }}>
      {t('live_card_title')}
    </div>
    <div style={{ fontSize: '13px', color: '#72726E' }}>
      {t('live_card_desc')}
    </div>
  </div>
  <button
    type="button"
    onClick={() => navigate('/live')}
    style={{
      backgroundColor: '#798C00',
      color: '#fff',
      border: 'none',
      borderRadius: '9999px',
      padding: '10px 20px',
      fontWeight: 600,
      fontSize: '13px',
      cursor: 'pointer',
      whiteSpace: 'nowrap',
    }}
  >
    {t('live_card_cta')} →
  </button>
</div>
```

- [ ] **Step 3: 确认编译无报错**

```bash
cd frontend && npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/pages/UploadPage.tsx
git commit -m "feat: add Live entry card in UploadPage"
```

---

## Task 5: 后端 — live.py 路由（WebSocket ASR + SSE 解释）

**Files:**
- Create: `backend/routers/live.py`
- Modify: `backend/main.py`

- [ ] **Step 1: 创建 backend/routers/live.py**

```python
"""
Live router.
  WebSocket /ws/live-asr  — 接收音频 chunk，转发阿里云流式 ASR，推回识别结果
  POST /api/live/explain  — 接收页码+转录文本，SSE 流式返回 Claude 解释
"""
import os
import json
import asyncio
from fastapi import APIRouter, WebSocket, WebSocketDisconnect
from fastapi.responses import StreamingResponse
from pydantic import BaseModel
import anthropic

router = APIRouter(tags=["live"])

# ── POST /api/live/explain ─────────────────────────────────────────────────────

class ExplainRequest(BaseModel):
    page_num: int
    ppt_text: str
    transcript: str

@router.post("/live/explain")
async def live_explain(req: ExplainRequest):
    """
    接收当前页的 PPT 文本 + 转录文本，调用 Claude，SSE 流式返回解释。
    """
    client = anthropic.Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])

    prompt = f"""你是一个课堂学习助手。请根据以下内容，用简洁的中文解释老师在这张幻灯片上讲解的核心内容。

幻灯片文字：
{req.ppt_text or "（无幻灯片文字）"}

老师录音转录：
{req.transcript or "（暂无转录内容）"}

要求：
- 3-5 句话，重点突出
- 如果转录内容充足，优先从转录中提炼
- 如果转录内容少，基于幻灯片文字补充
"""

    def generate():
        with client.messages.stream(
            model="claude-sonnet-4-6",
            max_tokens=512,
            messages=[{"role": "user", "content": prompt}],
        ) as stream:
            for text in stream.text_stream:
                yield f"data: {json.dumps({'text': text})}\n\n"
        yield "data: [DONE]\n\n"

    return StreamingResponse(generate(), media_type="text/event-stream")


# ── WebSocket /ws/live-asr ─────────────────────────────────────────────────────

@router.websocket("/ws/live-asr")
async def live_asr(websocket: WebSocket):
    """
    接收前端音频 chunk（binary），转发给阿里云 NLS 流式 ASR，
    将识别结果推回前端。

    当前实现：mock 模式（每收到 chunk 返回占位文字），
    阿里云 NLS 集成作为 TODO 在此函数内标记。
    """
    await websocket.accept()

    # 累积 chunk 计数（用于 mock 输出）
    chunk_count = 0

    try:
        while True:
            try:
                data = await asyncio.wait_for(websocket.receive_bytes(), timeout=30.0)
            except asyncio.TimeoutError:
                # 心跳超时，关闭连接
                break

            chunk_count += 1

            # TODO: 集成阿里云 NLS 流式 ASR
            # 参考文档：https://nls-portal.console.aliyun.com/
            # SDK：nls-python-sdk or websockets 直接连接
            # wss://nls-gateway.cn-shanghai.aliyuncs.com/ws/v1
            # 认证：token 通过 POST https://nls-meta.cn-shanghai.aliyuncs.com 获取
            #
            # 当前 mock：每 20 个 chunk（约 5 秒）返回一条占位识别结果
            if chunk_count % 20 == 0:
                result = {
                    "text": f"（模拟转录第 {chunk_count // 20} 句）",
                    "is_final": True,
                    "timestamp": chunk_count * 0.25,
                }
                await websocket.send_text(json.dumps(result))
            else:
                # 非 final 的实时预览（mock）
                result = {
                    "text": "…",
                    "is_final": False,
                    "timestamp": chunk_count * 0.25,
                }
                await websocket.send_text(json.dumps(result))

    except WebSocketDisconnect:
        pass
```

- [ ] **Step 2: 在 backend/main.py 挂载 live router**

在 `from routers import process, sessions, diagnostics` 改为：
```python
from routers import process, sessions, diagnostics, live
```

在 `app.include_router(diagnostics.router, prefix="/api")` 之后加入：
```python
app.include_router(live.router, prefix="/api")
```

注意：WebSocket 路由 `/ws/live-asr` 不加 `/api` prefix，因此 websocket 路由单独注册：

实际上 FastAPI 的 WebSocket 路由会跟随 prefix，所以前端连接地址应为 `ws://localhost:8000/api/ws/live-asr`。在 Task 6 中前端连接时使用此地址。

- [ ] **Step 3: 确认后端启动无报错**

```bash
cd backend && python -m uvicorn main:app --reload --port 8000
```

预期：看到 `Application startup complete.`，无 ImportError。

- [ ] **Step 4: 用 curl 测试 /api/live/explain**

```bash
curl -X POST http://localhost:8000/api/live/explain \
  -H "Content-Type: application/json" \
  -d '{"page_num":1,"ppt_text":"函数极限","transcript":"老师说极限就是趋近的概念"}' \
  --no-buffer
```

预期：看到 SSE 流式输出，格式为 `data: {"text": "..."}`，最后 `data: [DONE]`

- [ ] **Step 5: Commit**

```bash
git add backend/routers/live.py backend/main.py
git commit -m "feat: add live router - mock WebSocket ASR + SSE explain endpoint"
```

---

## Task 6: LivePage.tsx — fork NotesPage，替换核心逻辑

**Files:**
- Modify: `frontend/src/pages/LivePage.tsx`

这是核心任务。将 NotesPage.tsx 的完整内容复制到 LivePage.tsx，然后做以下改动：

- [ ] **Step 1: 将 NotesPage.tsx 完整内容复制到 LivePage.tsx**

```bash
cp frontend/src/pages/NotesPage.tsx frontend/src/pages/LivePage.tsx
```

然后将文件中所有 `export default function NotesPage` 改为 `export default function LivePage`。

- [ ] **Step 2: 移除 URL 参数依赖，改为固定 sessionId**

NotesPage 用 `useParams<{ sessionId: string }>()` 拿 sessionId。LivePage 课中没有 sessionId，需要在组件挂载时生成一个临时 ID：

将：
```tsx
const { sessionId } = useParams<{ sessionId: string }>()
```
改为：
```tsx
const sessionIdRef = useRef(`live-${Date.now()}`)
const sessionId = sessionIdRef.current
```

同时移除 `import { useParams } from 'react-router-dom'`（如果只用于 useParams，否则保留）。

- [ ] **Step 3: 移除静态 API 加载，新增 WebSocket 相关 state**

删除这段 useEffect（约在 L1010-L1019）：
```tsx
useEffect(() => {
  if (!sessionId) return
  getSession(sessionId)
    .then((data) => { ... })
    .catch(() => { ... })
}, [sessionId])
```

在 state 区域（`const [session, setSession]` 附近）做以下改动：

**保留** `session` state 但初始化为 null 且不从 API 加载（LivePage 课中没有笔记数据）。

**新增** 以下 state（在现有 state 声明之后）：
```tsx
const [wsStatus, setWsStatus] = useState<'idle' | 'connecting' | 'live' | 'stopped'>('idle')
const [subtitleLines, setSubtitleLines] = useState<string[]>([])
const [transcriptByPage, setTranscriptByPage] = useState<Record<number, string[]>>({})
const [explanationsByPage, setExplanationsByPage] = useState<Record<number, string>>({})
const [currentExplanation, setCurrentExplanation] = useState('')
const [explaining, setExplaining] = useState(false)
const wsRef = useRef<WebSocket | null>(null)
const mediaRecorderRef = useRef<MediaRecorder | null>(null)
const subtitleBottomRef = useRef<HTMLDivElement>(null)
```

- [ ] **Step 4: 新增录音 + WebSocket 逻辑**

在 state 声明之后、return 之前，加入以下函数：

```tsx
const API_BASE = import.meta.env.VITE_API_BASE_URL || 'http://localhost:8000'
const WS_BASE = API_BASE.replace(/^http/, 'ws')

const startRecording = useCallback(async () => {
  setWsStatus('connecting')
  try {
    const stream = await navigator.mediaDevices.getUserMedia({ audio: true })
    const ws = new WebSocket(`${WS_BASE}/api/ws/live-asr`)
    wsRef.current = ws

    ws.onopen = () => {
      setWsStatus('live')
      const mr = new MediaRecorder(stream, { mimeType: 'audio/webm' })
      mediaRecorderRef.current = mr
      mr.ondataavailable = (e) => {
        if (e.data.size > 0 && ws.readyState === WebSocket.OPEN) {
          ws.send(e.data)
        }
      }
      mr.start(250) // 每 250ms 产出一个 chunk
    }

    ws.onmessage = (e) => {
      const msg: { text: string; is_final: boolean; timestamp: number } = JSON.parse(e.data)
      if (msg.is_final) {
        setSubtitleLines(prev => {
          const next = [...prev, msg.text]
          return next.length > 50 ? next.slice(next.length - 50) : next
        })
        setTranscriptByPage(prev => ({
          ...prev,
          [currentPage]: [...(prev[currentPage] ?? []), msg.text],
        }))
      } else {
        // 非 final：更新最后一行为实时预览
        setSubtitleLines(prev => {
          if (prev.length === 0) return [msg.text]
          return [...prev.slice(0, -1), msg.text]
        })
      }
    }

    ws.onerror = () => setWsStatus('stopped')
    ws.onclose = () => setWsStatus('stopped')
  } catch {
    setWsStatus('idle')
  }
}, [currentPage])

const stopRecording = useCallback(async () => {
  mediaRecorderRef.current?.stop()
  wsRef.current?.close()
  setWsStatus('stopped')

  // 将 explanationsByPage 写入 IndexedDB
  const db = await openAskDB()
  await new Promise<void>((resolve) => {
    const tx = db.transaction(MY_NOTES_STORE, 'readwrite')
    tx.objectStore(MY_NOTES_STORE).put(
      { explanations: explanationsByPage },
      `live-explanations-${sessionId}`
    )
    tx.oncomplete = () => resolve()
    tx.onerror = () => resolve()
  })
}, [explanationsByPage, sessionId])

const handleExplainPage = useCallback(async () => {
  if (wsStatus !== 'live' && wsStatus !== 'stopped') return
  setExplaining(true)
  setCurrentExplanation('')

  const transcript = (transcriptByPage[currentPage] ?? []).join('')
  // ppt_text 暂时为空（LivePage 课中无 PPT 解析数据）
  const pptText = ''

  try {
    const res = await fetch(`${API_BASE}/api/live/explain`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ page_num: currentPage, ppt_text: pptText, transcript }),
    })
    if (!res.body) return
    const reader = res.body.getReader()
    const decoder = new TextDecoder()
    let full = ''
    while (true) {
      const { done, value } = await reader.read()
      if (done) break
      const chunk = decoder.decode(value)
      for (const line of chunk.split('\n')) {
        if (line.startsWith('data: ')) {
          const payload = line.slice(6).trim()
          if (payload === '[DONE]') break
          try {
            const { text } = JSON.parse(payload)
            full += text
            setCurrentExplanation(full)
          } catch {}
        }
      }
    }
    setExplanationsByPage(prev => ({ ...prev, [currentPage]: full }))
  } finally {
    setExplaining(false)
  }
}, [wsStatus, currentPage, transcriptByPage, API_BASE])
```

- [ ] **Step 5: 字幕条自动滚动 useEffect**

在 `useEffect` 集中区域加入：

```tsx
useEffect(() => {
  subtitleBottomRef.current?.scrollIntoView({ behavior: 'smooth' })
}, [subtitleLines])
```

- [ ] **Step 6: 修改右侧面板（替换 noteMode tab 为 AI 解释区）**

在 NotesPage 中，右侧面板渲染了 `my / ai / transcript` 三个 tab。在 LivePage 中，找到右侧面板的 JSX（搜索 `noteMode === 'ai'` 或 `notes_ai_tab`），将整个右侧面板内容替换为：

```tsx
{/* LivePage 右侧：AI 解释区 */}
<div style={{ display: 'flex', flexDirection: 'column', height: '100%', padding: '16px', gap: '12px' }}>
  <div style={{ fontWeight: 700, fontSize: '13px', letterSpacing: '0.1em', color: '#72726E', textTransform: 'uppercase' }}>
    {t('live_explain_empty').split('，')[0]}
  </div>

  <div style={{ flex: 1, overflowY: 'auto', fontSize: '14px', lineHeight: '1.7', color: '#2F3331' }}>
    {explaining ? (
      <span style={{ whiteSpace: 'pre-wrap' }}>{currentExplanation}<span style={{ opacity: 0.5 }}>▋</span></span>
    ) : explanationsByPage[currentPage] ? (
      <span style={{ whiteSpace: 'pre-wrap' }}>{explanationsByPage[currentPage]}</span>
    ) : (
      <span style={{ color: '#B0AFA7' }}>{t('live_explain_empty')}</span>
    )}
  </div>

  <button
    type="button"
    disabled={wsStatus === 'idle' || wsStatus === 'connecting' || explaining}
    onClick={handleExplainPage}
    title={wsStatus === 'idle' ? t('live_recording_required') : undefined}
    style={{
      backgroundColor: wsStatus === 'idle' || explaining ? '#D0CFC5' : '#798C00',
      color: '#fff',
      border: 'none',
      borderRadius: '9999px',
      padding: '10px 0',
      fontWeight: 600,
      fontSize: '13px',
      cursor: wsStatus === 'idle' || explaining ? 'not-allowed' : 'pointer',
      width: '100%',
    }}
  >
    {explaining ? '…' : explanationsByPage[currentPage] ? t('live_explain_refresh') : t('live_explain_btn')}
  </button>
</div>
```

- [ ] **Step 7: 在 PPT 画布下方插入字幕条**

找到中间列 PPT 画布渲染区域的底部（搜索 `canvasAreaRef` 所在的 div 结束处），在其**下方**插入字幕条：

```tsx
{/* 字幕条 */}
<div
  style={{
    height: '120px',
    overflowY: 'auto',
    backgroundColor: 'rgba(30,30,30,0.85)',
    borderRadius: '8px',
    padding: '10px 16px',
    margin: '8px 16px',
    fontSize: '13px',
    lineHeight: '1.6',
    color: '#E8E8E0',
  }}
>
  {subtitleLines.length === 0 ? (
    <span style={{ opacity: 0.45 }}>{t('live_subtitle_placeholder')}</span>
  ) : (
    subtitleLines.map((line, i) => (
      <p key={i} style={{ margin: '2px 0' }}>{line}</p>
    ))
  )}
  <div ref={subtitleBottomRef} />
</div>
```

- [ ] **Step 8: 替换录音控制条（移除 noteMode PillToggle，加入录音按钮）**

找到 PillToggle 渲染（搜索 `<PillToggle`），在 LivePage 中将其替换为录音状态 + 控制按钮：

```tsx
{/* LivePage 录音控制条 */}
<div style={{ display: 'flex', alignItems: 'center', gap: '12px', padding: '8px 16px' }}>
  <span style={{
    fontSize: '11px',
    fontWeight: 700,
    letterSpacing: '0.12em',
    color: wsStatus === 'live' ? '#798C00' : '#B0AFA7',
    textTransform: 'uppercase',
  }}>
    {wsStatus === 'live' ? t('live_recording_label') :
     wsStatus === 'stopped' ? t('live_stopped_label') :
     t('live_idle_label')}
  </span>
  {wsStatus === 'idle' && (
    <button
      onClick={startRecording}
      style={{ backgroundColor: '#798C00', color: '#fff', border: 'none', borderRadius: '9999px', padding: '6px 16px', fontSize: '12px', fontWeight: 600, cursor: 'pointer' }}
    >
      ● {t('live_recording_label')}
    </button>
  )}
  {wsStatus === 'live' && (
    <button
      onClick={stopRecording}
      style={{ backgroundColor: '#D0CFC5', color: '#2F3331', border: 'none', borderRadius: '9999px', padding: '6px 16px', fontSize: '12px', fontWeight: 600, cursor: 'pointer' }}
    >
      ■ {t('live_stopped_label')}
    </button>
  )}
</div>
```

- [ ] **Step 9: 确认 TypeScript 编译无报错**

```bash
cd frontend && npx tsc --noEmit
```

预期：0 errors

- [ ] **Step 10: Commit**

```bash
git add frontend/src/pages/LivePage.tsx
git commit -m "feat: implement LivePage - WebSocket ASR, subtitle bar, AI explain panel"
```

---

## Task 7: 录音结束 → 跳转 ProcessingPage

**Files:**
- Modify: `frontend/src/pages/LivePage.tsx`

- [ ] **Step 1: 在 stopRecording 函数末尾，停止后跳转 ProcessingPage**

找到 Task 6 中写的 `stopRecording` 函数，在 IndexedDB 写入之后加入：

```tsx
// 跳转 ProcessingPage（无文件时，navigate 并带 state 标记为 live session）
navigate('/processing', { state: { sessionId, fromLive: true } })
```

同时在组件顶部确认有：
```tsx
const navigate = useNavigate()
```

- [ ] **Step 2: 确认 ProcessingPage 对 fromLive 无需修改**

ProcessingPage 目前通过 URL 参数或 state 判断进度，`fromLive: true` 不影响现有逻辑（只是标记，后续可用）。无需修改 ProcessingPage。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/pages/LivePage.tsx
git commit -m "feat: navigate to /processing after live recording stops"
```

---

## Task 8: 手动验证完整流程

**无代码修改，纯验证步骤。**

- [ ] **Step 1: 启动前后端**

```bash
# 终端 1 — 后端
cd backend && python -m uvicorn main:app --reload --port 8000

# 终端 2 — 前端
cd frontend && npm run dev
```

- [ ] **Step 2: 验证入口路径**

1. 打开 `http://localhost:5173`（LobbyPage）
2. 点击左侧绿色按钮 → 应跳转到 `/upload`（不弹 Modal）
3. 在 UploadPage 看到 Live 入口卡片
4. 点击「进入直播课堂」→ 应跳转到 `/live`

- [ ] **Step 3: 验证 LivePage 录音功能**

1. 在 `/live` 页面，点击录音按钮
2. 浏览器弹出麦克风权限 → 允许
3. 状态变为「录音中」
4. 字幕条约 5 秒后出现 mock 字幕「（模拟转录第 1 句）」
5. 左侧缩略图翻页（目前无 PPT 数据，可能为空）→ 切换 currentPage

- [ ] **Step 4: 验证 AI 解释**

1. 录音中，点击右侧「解释这页」
2. 右侧面板出现流式文字输出
3. 完成后显示解释文字

- [ ] **Step 5: 验证停止录音**

1. 点击「■ 已停止」按钮
2. 页面跳转到 `/processing`

- [ ] **Step 6: Commit（如有修复）**

```bash
git add -p
git commit -m "fix: live page integration issues found during manual testing"
```
