# LiberStudy PRD

> 版本: v0.2 | 作者: [Liber] | 日期: 2026-04-07 | 状态: Draft
> 目标：MVP-0 搭建（最小可演示版本）

---

## 1. Executive Summary

我们正在构建 **LiberStudy** —— 一款面向**上课场景**（未来扩展至会议）的课堂知识结构化工具，解决的核心问题是：**学生在课堂中无法完整采集结构化信息，课后只能靠低效的视频回放来复习**。

**MVP-0 聚焦场景②（课中实时录音）**，这是最小可演示闭环：

- **场景②（课中实时录音）[P0 - MVP-0 核心]**：课中实时录音（麦克风）+ 用户手动上传 PPT，用户以"图钉批注"方式点击 PPT 画面任意位置记录笔记（自动附带时间戳和页码锚点），课后批量生成结构化笔记
- **场景①（课后音频）[P1 - 尽力做]**：课后上传录音文件（MP3/WAV/M4A）+ 用户手动上传 PPT，系统 ASR 转录后生成结构化笔记
- **场景③④（视频/网课）[V2 - 暂不启动]**：详见附录 A

所有场景共用同一套输出逻辑：ASR 高精度转录 + 多模态对齐，批量生成两类笔记——**被动学习笔记**（PPT 框架 + 老师讲解细节结构化，所有页面均生成）和**主动学习笔记**（用户课中手写笔记 + 对应逐字稿 LLM 扩写，有用户笔记的页面额外叠加生成）。

LiberStudy 的目标是让学生课后无需反复回看视频，就能获得以 PPT 单页为粒度的结构化知识笔记，后续可无缝扩展为 RAG 智能问答和自动复习闪卡的高质量语料底座。

---

## 2. Problem Statement

### 谁有这个问题？

- **大学生**：每学期面对 10+ 门课程，大量使用 PPT 教学，课后需要消化密集的知识内容
- **职场人士**：频繁参加使用 PPT 的会议、培训、分享会，会后需要整理行动项和关键决策

### 问题是什么？

用户在课堂/会议中同时面对 PPT 画面和讲者口语讲解两个信息流，但现有工具只能分别处理其中之一：

1. **录音转文字工具**（如讯飞听见、Otter.ai）：只有文字流，丢失了 PPT 上的图表、公式、关键术语等视觉信息，且口语噪音大（"嗯"、"那个"、重复表述），可读性差
2. **PPT 本身**：只有静态画面，缺少讲者的解释、补充说明和上下文展开
3. **手动记笔记**：跟不上讲课节奏，记录不完整，且无法在课后重建"讲到这一页时老师说了什么"的关联；即便有录音，课后还要手动把录音内容和 PPT 页面一一对应，耗时费力

这导致用户不得不反复拖动进度条回看视频，花费 **2-3 倍的原始时长** 才能完成一次有效复习，而且整理出的笔记仍然是碎片化的、缺乏结构的。更上游的问题是：**课堂中就没有采集到结构化信息**，缺乏高质量原料，导致课后复习只能依赖低效的视频回放。

### 为什么这个问题很痛？

- **用户层面**：回看效率极低，知识碎片化导致复习效果差，考试/汇报前临时抱佛脚效率低下
- **时间成本**：一节 90 分钟的课，回看整理可能需要 2-4 小时
- **现有替代方案不解决根本问题**：飞书妙记/Otter.ai 只做转录不做结构化；Notion AI 需要用户手动整理；录屏软件只是把问题从"课堂"搬到了"屏幕前"

### 证据（待补充 — 建议通过用户访谈验证）

- **痛点假设**：大学生群体中"课后回看效率低"是高频吐槽（知乎、小红书、B站弹幕中常见"2倍速都看不完"的表述）
- **行为信号**：B站/网课平台上课程视频的完播率普遍低于 30%，暗示用户难以坚持完整回看
- **市场信号**：AI 学习笔记类产品（NotebookLM、Gamma、ChatGPT 搭配 PDF）在 2025-2026 年快速增长，说明需求真实存在
- **关键假设需验证**："以 PPT 单页为锚点的结构化笔记"是否比"全文转录 + AI 总结"更符合用户的复习心智模型？

---

## 3. Target Users & Personas

### Primary Persona: 理工科大学生"小林"

| 维度 | 描述 |
|------|------|
| **角色** | 985/211 高校大三学生，计算机/电子/自动化相关专业 |
| **课程特点** | 每学期 8-12 门专业课，PPT 含大量公式、代码片段、系统架构图 |
| **技术素养** | 较高，熟悉 AI 工具（ChatGPT、Kimi），愿意尝试新产品 |
| **目标** | 用最少时间把课堂知识消化到位，期末/考研高效复习 |
| **痛点** | 老师讲的推导过程和补充说明不在 PPT 上；录音事后无法高效利用，手动对应到 PPT 页面耗时费力；手写笔记跟不上公式板书速度 |
| **当前行为** | 上课时用录音笔/手机录音 → 考前花整天回放，手动把录音对应到 PPT 页 → 对着 PPT 补笔记 → 效果不好仍然焦虑 |
| **成功标准** | 课后 10 分钟内获得一份"每页 PPT + 老师讲了什么"的结构化笔记 |

### Secondary Persona: 文商科大学生"小陈"

| 维度 | 描述 |
|------|------|
| **角色** | 本科大二，经管/法学/新闻相关专业 |
| **课程特点** | PPT 以文字、概念框架、案例为主，老师口头补充大量案例和观点 |
| **技术素养** | 中等，使用笔记类 App（Notion、飞书文档），对 AI 工具有基本认知 |
| **目标** | 整理出体系化的课程笔记，支撑论文写作和期末论述题 |
| **痛点** | 老师讲的精华（案例、个人见解、延伸阅读建议）全在口语里，PPT 只有提纲 |
| **当前行为** | 上课拍 PPT 照片 → 听课时尽量手写 → 课后拼凑，经常遗漏关键论述 |

### Tertiary Persona: 职场人士"Alex"（MVP 后期扩展）

| 维度 | 描述 |
|------|------|
| **角色** | 互联网/咨询公司 3-5 年经验，频繁参加 PPT 驱动的会议和内部培训 |
| **课程特点** | 项目汇报、方案评审、技术分享会，PPT 含数据图表和业务逻辑 |
| **目标** | 会后快速输出结构化会议纪要，提取 action items |
| **痛点** | 开会时忙于讨论无暇记录，会后凭记忆整理纪要不准确 |

### Jobs-to-be-Done (JTBD)

| 场景 | 用户想要... | 以便于... |
|------|------------|----------|
| **上课时** | 轻松采集课堂音频和结构化信息（录音+PPT+笔记） | 课后有高质量原料生成笔记，无需反复回看视频 |
| 课后/会后 | 快速获得按 PPT 页面组织的结构化笔记 | 不用回看视频就能掌握全部关键内容 |
| 复习备考 | 按知识点检索"老师在这页讲了什么" | 精准定位薄弱环节，高效复习 |
| 知识沉淀 | 把课堂/会议内容变成可检索的知识库 | 未来随时调用，支撑学习和工作 |

---

## 4. Strategic Context

### 业务目标

这是一个从 0 到 1 的概念验证项目，核心目标是：

1. **验证产品假设**：证明"PPT 锚点 + 多模态对齐"的结构化笔记方案，在用户体验上显著优于纯转录 + AI 总结方案
2. **构建个人产品作品集**：以 Vibe Coding 方式完成从需求定义到可交互产品的全链路，展示 AI 产品经理的核心能力
3. **积累技术壁垒认知**：通过实际构建，深入理解多模态对齐、ASR 降噪、知识结构化等 AI 技术在产品中的落地挑战

### 市场机会

| 层级 | 规模 | 说明 |
|------|------|------|
| **TAM** | ~3 亿人 | 中国在校大学生（~4000 万）+ 全球使用 PPT 开会的知识工作者 |
| **SAM** | ~5000 万人 | 有课后回看/会后整理习惯，且对 AI 工具有认知的用户 |
| **SOM** | ~50-100 万人 | 第一阶段：中国 985/211 高校理工科学生 + 互联网行业职场人 |

### 竞品分析

| 竞品 | 做了什么 | 没做什么（LiberStudy 的差异点） |
|------|---------|-------------------------------|
| **飞书妙记** | 会议录音 → 转录 → AI 总结 | 不做 PPT 页面级对齐；总结是全文维度的，不是按页结构化的 |
| **Otter.ai** | 实时转录 + AI 总结 + 搜索 | 纯文字流，不处理 PPT 视觉信息；无"哪一页讲了什么"的锚点 |
| **Google NotebookLM** | 上传文档 → 对话式问答 | 不处理视频/音频输入；不做时间轴对齐 |
| **Gamma / 通义听悟** | AI 生成 PPT / 音视频转文字 | Gamma 做 PPT 生成而非理解；通义听悟做转录但不做 PPT 对齐 |
| **手动方式** | 录屏 + 手写笔记 | 耗时长、不完整、不可检索、无结构 |

**LiberStudy 的核心差异：** 唯一以"PPT 单页"为空间锚点，将视觉信息（PPT 内容）和语音信息（讲者口语）做多模态对齐后结构化输出的产品。支持上课实时录音采集，不强制要求用户事后上传录屏视频，大幅降低使用门槛。产品区分被动学习（系统自动生成）和主动学习（基于用户笔记扩写），从而满足不同参与度的学生。这不是"转录 + 总结"，而是"知识结构化重塑"。

### Why Now?

1. **多模态大模型成熟**：GPT-4o / Claude 4.x / Qwen-VL 等模型已具备同时理解图片和文字的能力，使得"PPT 画面理解 + 转录文字融合"在技术上首次可行
2. **ASR 成本大幅下降**：Whisper 开源后，语音转文字成本降至几乎为零，不再是瓶颈
3. **AI 原生产品认知普及**：目标用户（大学生、互联网从业者）对 AI 工具接受度高，无需教育市场
4. **竞品空白窗口**：现有产品都在"转录"或"总结"赛道上竞争，且均要求用户课后上传录屏视频，用户往往不主动录屏导致工具门槛高；尚无产品真正做到 PPT 页面级的多模态知识对齐，也没有产品支持上课实时录音采集

---

## 5. Solution Overview

### 产品形态

Web 应用（响应式设计，优先桌面端体验）。产品分为两个使用阶段：**上课阶段**（音频采集 + PPT + 笔记输入）和**课后阶段**（批量生成结构化笔记）。

### UI 设计原则与工具

**UI 设计工具**：使用 Figma（配合 Figma MCP + Claude Code）进行 UI 设计，设计稿作为前端开发的直接依据。

**MVP UI 目标**：最简可验证全流程，不追求视觉精美，核心是功能闭环跑通。

**MVP UI 最简设计约束**

| 页面/模块 | 最简实现要求 |
|----------|------------|
| **上课采集界面（场景②）** | 三栏布局（与课后查看界面一致）：左栏为可折叠大纲导航，中栏展示当前 PPT 页面，右栏为笔记批注区 + 录音控制条（顶部固定，含开始/暂停/停止按钮 + 录音时长计时）；用户点击中栏 PPT 画面任意位置生成"图钉"，右栏同步新建笔记卡片并以贝塞尔曲线连接，用户在卡片中输入文字笔记 |
| **文件上传界面（场景①）** | 两个上传区域并排（PPT 文件 / 音频文件），拖拽或点击选择，上传完成后显示文件名和"开始处理"按钮 |
| **处理进度界面** | 线性进度条 + 当前阶段文字说明（语音转录中 / PPT 内容解析中 / 笔记生成中），显示预估剩余时间 |
| **课后笔记查看界面** | 三栏布局：左栏为可折叠大纲导航（Hover 浮出缩略图列表，点击跳转）；中栏为 PPT 主画布（展示用户图钉标记，画布上伸出贝塞尔曲线连至右侧笔记卡片）；右栏为动态笔记区（右上角 [我的笔记 | AI笔记] 视图切换器，卡片含 bullet 行级注释 + 引用时间戳 + page_supplement；点击时间戳音频跳转；对齐置信度低的页面显示 ⚠️） |

**Figma 设计工作流**：
1. 先用 Figma 设计各页面线框图（Wireframe），在 Claude Code 中通过 Figma MCP 读取设计稿
2. 以线框图为基准生成前端代码
3. UI 评审后再出高保真视觉稿（V1.1 阶段），MVP 阶段以功能跑通为准

### MVP-0 输入场景

| 场景 | 音频来源 | PPT 来源 | PPT 翻页时间轴构建方式 | 优先级 |
|------|---------|---------|----------------------|--------|
| **场景②：课中实时录音** | Web 端麦克风实时采集 | 用户上传 PPT/PPTX | 用户上课时以"图钉批注"方式点击 PPT 画面记录笔记，系统自动记录时间戳和 Y 轴位置 | **P0** |
| **场景①：课后上传录音文件** | 用户上传 MP3/WAV/M4A | 用户上传 PPT/PPTX | 用户可选手动打点页码-时间戳，未打点时语义推断 | P1 |

> 场景③（课后上传录频文件）和场景④（实时网课监听）移至附录 A，V2 阶段启动。

### 核心用户流程

```
阶段一：上课采集

输入来源（两种）：
  ② 课中实时录音（麦克风）[P0]
  ① 录音文件上传（MP3/WAV/M4A）[P1]
        +
PPT 来源：用户上传 PPTX
        +
用户笔记输入（场景②时有效，其他场景课后补充）：
  - 图钉（Pin）：点击中栏 PPT 画面任意位置生成图钉，记录精确的 Y 轴坐标位置
  - 笔记卡片：图钉触发右栏新建卡片，用户在卡片中输入关键词、疑问、要点
  - 时间戳 + 页码：系统自动附加录音相对时间和当前页码
        │
        ▼
  输出：录音/音轨 + 用户笔记（含页码+时间戳）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

阶段二：课后批量生成

音轨 + 用户笔记 + PPT 内容
        │
        ▼
  ┌───────────────────────────────────┐
  │  Step 1: ASR 高精度转录            │
  │  将音轨转为带时间戳的逐字稿          │
  └───────────────────────────────────┘
        │
        ▼
  ┌───────────────────────────────────┐
  │  Step 2: 翻页时间轴构建             │
  │  场景②：用户笔记防抖锚点（仅供参考）│
  │          + 逐字稿语义相似度校准     │
  │  场景①：用户可选手动打点（页码-    │
  │          时间戳），未打点按语义推断  │
  │  注：场景②支持一页对多不连续片段   │
  └───────────────────────────────────┘
        │
        ▼
  ┌───────────────────────────────────┐
  │  Step 3【被动学习】（所有页面）      │
  │  PPT 每页 bullet + 对应逐字稿片段   │
  │  → LLM 行级对齐：为每个 bullet 生成 │
  │    注释 + 引用时间戳                 │
  │  → 超出 bullet 范围的讲解归入        │
  │    page_supplement 字段             │
  │  （MVP 仅提供逐字稿内容，不含 RAG） │
  └───────────────────────────────────┘
        │
        ▼
  ┌───────────────────────────────────┐
  │  Step 4【主动学习】（有用户笔记时）  │
  │  在被动学习基础上叠加生成：           │
  │  用户原始笔记 + 对应逐字稿           │
  │  → LLM 扩写用户笔记                 │
  │  （课后批量触发）                    │
  └───────────────────────────────────┘
        │
        ▼
  输出：
  • 每页 PPT：逐 bullet 行级注释 + 引用时间戳（被动学习，所有页面）
  • 每页 PPT：page_supplement 补充说明（老师脱离课件的讲解）
  • 有用户笔记的页面额外叠加：扩写后的用户笔记（主动学习）
```

### 关键特性（MVP-0）

| 特性 | 描述 | 适用场景 | 优先级 |
|------|------|---------|--------|
| **PPT 文件上传** | 支持上传 PPT/PPTX，大小限制 ≤ 100MB | 场景①② | P0 |
| **课中实时录音** | Web 端麦克风实时录制，支持开始/暂停/停止 | 场景② | P0 |
| **用户笔记输入（图钉批注）** | 用户点击 PPT 画面任意位置生成图钉，右侧同步新建笔记卡片（贝塞尔曲线连接），系统自动记录页码 + 录音时间戳 + Y 轴位置；MVP 仅支持键盘输入 | 场景②为主 | P0 |
| **ASR 高精度转录** | 将音频转为带时间戳逐字稿 | 场景①② | P0 |
| **PPT 翻页时间轴（场景②）** | 用户笔记防抖锚点（参考，非硬边界）+ 语义相似度校准，支持非线性讲解 | 场景② | P0 |
| **被动学习笔记生成** | 课后批量：PPT 每页 bullet + 对应逐字稿 → 行级 LLM 对齐，输出每个 bullet 的注释 + 引用时间戳 + page_supplement 补充说明（仅逐字稿内容，不含 RAG）；**所有页面均生成**，在 AI 增强笔记模式（默认）下展示完整结构化卡片 | 场景①② | P0 |
| **主动学习笔记扩写与双态视图** | 课后批量：用户图钉骨架笔记 + 逐字稿 → LLM 扩写；**在被动学习基础上叠加**，有用户笔记的页面同时展示 bullet 注释和扩写笔记；右上角 [我的笔记 \| AI笔记] 切换器 | 场景② | P0 |
| **录音文件上传** | 支持上传 MP3/WAV/M4A 录音文件 | 场景① | P1 |
| **PPT 翻页时间轴（场景①）** | 用户可选手动打点页码-时间戳，未打点时语义推断（具体算法待技术方案验证后确定） | 场景① | P1 |
| **笔记导出** | 支持 Markdown / PDF 导出 | 场景①② | P1 |

### 技术方案选型（已决策）

**MVP 采用混合方案**，以下为确定的技术选型：

#### 系统架构

```
用户浏览器（前端）
    ↓ HTTP / WebSocket
本地后端服务（FastAPI / Python）
    ↓
┌───────────────────────────────────┐
│  ASR（语音转文字）                  │
│  中文 → 阿里云 ASR API             │
│  英文 → OpenAI Whisper API        │
├───────────────────────────────────┤
│  课件解析与渲染                      │
│  LibreOffice 统一转 PDF             │
│  → PyMuPDF 提取每页文本+布局        │
│  → PDF 渲染为 PNG（缩略图+画布）    │
│  支持输入：.ppt / .pptx / .pdf     │
├───────────────────────────────────┤
│  笔记生成（LLM）                    │
│  Claude API（claude-sonnet）       │
│  输入：PPT 页面内容 + 对应逐字稿    │
│  输出：结构化知识笔记               │
└───────────────────────────────────┘
```

#### 技术栈

| 层级 | 技术选型 | 理由 |
|------|---------|------|
| **前端** | React + Vite + Tailwind CSS + shadcn/ui | 生态成熟，组件库专业，适合快速构建高质量 UI |
| **后端** | FastAPI（Python） | Python 生态对 AI/文件处理库支持最好，异步性能佳 |
| **课件解析与渲染** | LibreOffice + PyMuPDF | 所有输入（.ppt/.pptx/.pdf）统一转 PDF；PyMuPDF 提取文本+布局并渲染为 PNG；格式兼容性最强 |
| **音频格式转换** | FFmpeg | 浏览器录音产出 WebM/Opus，后端用 FFmpeg 统一转为 WAV/MP3 再送 ASR API；同时处理用户上传的 M4A 等格式 |
| **中文 ASR** | 阿里云智能语音 API | 中文精度高，有 10 小时/月免费额度 |
| **英文 ASR** | OpenAI Whisper API | 英文精度最优，接入简单 |
| **笔记生成** | Claude API（Anthropic，claude-sonnet） | 长文本处理能力强，结构化输出稳定 |
| **语义对齐** | OpenAI text-embedding-3-small | 中英文效果均优，$0.02/1M tokens 成本极低；复用 OpenAI API Key |
| **运行方式** | 本地运行（localhost），API Key 存于 .env | MVP 定位为面试演示项目，无需服务器部署 |

#### 部署方式

MVP 为**本地运行模式**，面试官克隆仓库后填入 API Key，一条命令启动：

```bash
# 1. 克隆仓库
git clone https://github.com/[user]/LiberStudy.git

# 2. 复制 .env 模板并填入 API Key
cp .env.example .env

# 3. 启动（前后端同时）
make dev
```

GitHub 仓库包含所有源代码 + `.env.example`（模板，Key 值为空，真实 Key 不上传）。

> **MVP-0 范围说明**：当前版本核心保障场景②（课中实时录音），场景①（录音文件上传）作为 P1 尽力实现。课件文件（.ppt/.pptx/.pdf）统一由 LibreOffice 转 PDF 后，PyMuPDF 提取文本并渲染为 PNG。场景③④移至附录 A。

---

### API 成本面板

#### ASR 成本（语音转文字）

| 语言 | 服务 | 单价 | 90 分钟课程成本 | 免费额度 |
|------|------|------|-------------|---------|
| **中文** | 阿里云智能语音（录音文件转写） | ¥0.001/秒 | ≈ ¥5.4 | 10 小时/月（≈ 6 节课/月免费） |
| **英文** | OpenAI Whisper API | $0.006/分钟 | ≈ $0.54（¥3.9） | 无 |

#### 笔记生成成本（LLM）

以 Claude claude-sonnet 为例，按每页 PPT 平均输入 1500 tokens、输出 800 tokens 估算：

| PPT 页数 | 输入 tokens | 输出 tokens | 估算成本 |
|---------|-----------|-----------|---------|
| 20 页 | ~30,000 | ~16,000 | ≈ $0.13（¥0.9） |
| 40 页 | ~60,000 | ~32,000 | ≈ $0.26（¥1.9） |
| 60 页 | ~90,000 | ~48,000 | ≈ $0.39（¥2.8） |

> 定价基于 Claude claude-sonnet：输入 $3/MTok，输出 $15/MTok（2026 年 4 月）

#### 单节课总成本估算

| 场景 | ASR | LLM（40页PPT） | 合计 |
|------|-----|--------------|------|
| 90 分钟中文课（超出免费额度后） | ¥5.4 | ¥1.9 | **≈ ¥7.3/节** |
| 90 分钟中文课（免费额度内） | ¥0 | ¥1.9 | **≈ ¥1.9/节** |
| 90 分钟英文课 | ¥3.9 | ¥1.9 | **≈ ¥5.8/节** |

#### MVP 成本控制措施

- 每用户每天最多处理 2 次，单次音频最长 120 分钟
- 中文 ASR 优先消耗阿里云免费额度（10 小时/月）
- LLM 并发调用上限 ≤ 5，避免短时突发费用

#### 场景②实时录音链路额外成本

场景②的录音在浏览器端采集，课后批量上传至本地后端进行 ASR。MVP 阶段为本地运行（localhost），音频 Blob 传输无带宽成本。若未来上云部署，需额外考虑：

| 项目 | 估算 | 说明 |
|------|------|------|
| **音频上传带宽** | ~50-80MB/90分钟（WebM Opus 编码） | 本地运行无成本；云端约 ¥0.5/节（按 CDN 流量计费） |
| **临时存储** | ~50-80MB/任务 | 本地运行使用磁盘临时目录；云端需对象存储，处理完成后清理 |
| **音频分片传输** | 无额外成本 | 前端每 10 分钟切片，课后并发上传，仅影响网络 I/O |

---

## 6. Success Metrics

### Primary Metric

**笔记可用率**：用户生成笔记后，实际用于复习/工作（而非弃用）的比例

- **定义**：生成笔记后 7 天内至少重新打开查看 1 次的用户比例
- **目标**：≥ 60%（MVP 阶段）
- **衡量方式**：MVP 为本地运行，无远程埋点。通过以下方式收集：① 产品内轻量反馈弹窗（生成笔记后弹出 1-5 分评价，结果存入 IndexedDB）；② 用户访谈（5-10 位目标用户的定性反馈）。目标值作为定性验证基准，非精确统计

### Secondary Metrics

| 指标 | 定义 | 目标 |
|------|------|------|
| **处理成功率** | 任一输入场景触发处理 → 成功生成笔记的比例 | ≥ 90% |
| **端到端处理时间** | 从上传到笔记可查看的等待时间 | 90 分钟课程 ≤ 10 分钟 |
| **用户满意度** | 对生成笔记质量的 1-5 分评分（产品内反馈） | 平均 ≥ 3.5 分 |
| **导出率** | 生成笔记后导出为 PDF 的比例 | ≥ 30% |
| **复访率** | 7 天内回访率 | ≥ 40% |

### 技术验证指标（三方案对比专用）

| 指标 | 说明 | 评判标准 |
|------|------|---------|
| **笔记质量得分** | 人工盲评：完整性、准确性、可读性（各 1-5 分） | 方案间差异 ≤ 0.5 分则选成本更低的方案 |
| **单课成本** | 处理一节 90 分钟课程的直接成本 | 方案 A: ~¥5-10，方案 B: ~¥0.5-1，方案 C: ~¥2-5 |
| **处理延迟** | 从录音结束（或文件上传完成）到笔记可查看的端到端延迟 | 目标 ≤ 10 分钟/90 分钟课程 |
| **ASR 精度** | 逐字稿字错率（WER），在专业术语密集场景下的准确率 | 目标 WER ≤ 10%（中文）|
| **PPT 类型泛化性** | 在公式密集/文字密集/图表密集三类 PPT 上的被动学习笔记得分一致性 | 各类型得分标准差 ≤ 0.5 |

### Guardrail Metrics

- **隐私安全**：用户上传的录音、录频和 PPT 不被泄露或用于模型训练（使用 API 时需确认数据政策）
- **误导率**：生成笔记中出现与原始内容矛盾的信息比例 ≤ 5%（通过抽样人工审核）

---

## 7. User Stories & Requirements

### Epic Hypothesis

我们相信，通过构建"课中实时录音采集（场景②）+ 课后批量生成结构化笔记（被动学习 + 主动学习叠加）"这一核心链路，能让大学生课后无需反复回看视频，就能获得以 PPT 单页为粒度的结构化知识笔记，从而将复习效率提升 2-3 倍。我们将在 MVP-0 上线后 30 天内，通过**笔记可用率 ≥ 60%** 和**用户满意度平均分 ≥ 3.5** 来验证这一假设。

---

### Story 1A：课中实时录音（场景②）

**As** 大学生小林，**I want** 上课时在浏览器中启动录音并在 PPT 页面上直接输入笔记，**so that** 系统能采集完整的课堂音频和我的笔记，课后自动帮我生成结构化笔记。

**Acceptance Criteria:**
- [ ] 用户可预先上传 `.ppt` / `.pptx` / `.pdf`文件，大小限制 ≤ 100MB
- [ ] 支持 Web 端麦克风录音：一键开始、暂停/继续、结束录音
- [ ] 录音界面采用三栏布局：左栏可折叠大纲导航，中栏 PPT 画布，右栏笔记批注区（顶部含录音控制条）
- [ ] 用户点击中栏 PPT 画面任意位置，立即在该位置生成图钉图标，右栏同步新建笔记卡片并获取焦点，两者以贝塞尔曲线连接
- [ ] 每张笔记卡片自动记录：当前所在 PPT 页码 + 当前录音相对时间戳 + 图钉的 Y 轴位置信息
- [ ] 用户可自由翻页浏览 PPT，切换后生成的图钉自动对应新页码
- [ ] 录音结束后，点击"生成笔记"触发课后处理流程
- [ ] 录音过程中麦克风权限被拒绝时，提示用户如何开启并引导至录音文件上传模式

---

### Story 1B：课后上传录音文件（场景①）

**As** 小林，**I want** 上传我在课堂上录制的音频文件和对应 PPT，**so that** 系统能生成按 PPT 页面组织的结构化笔记。

**Acceptance Criteria:**
- [ ] 支持上传 `.mp3` / `.wav` / `.m4a` 音频文件，大小限制 ≤ 2GB
- [ ] 支持上传 `.ppt` / `.pptx` 文件，大小限制 ≤ 100MB
- [ ] 两个文件均上传成功后，"开始处理"按钮才可点击
- [ ] 文件上传过程中显示进度条（百分比）
- [ ] 上传失败时展示明确错误原因（格式不支持 / 超出大小限制 / 网络中断）
- [ ] 上传完成后，提供可选步骤：用户手动标记"某时间戳对应第几页 PPT"的对应关系（如未标记，系统按音频时长均分页面）
- [ ] 支持拖拽上传和点击选择两种方式

---

> 场景③（课后上传录频文件）和场景④（实时网课监听）的 User Stories 已移至**附录 A**。

---

### Story 2：查看处理进度

**As** 用户，**I want** 在等待处理时了解当前进度，**so that** 我知道还需要等多久，不会因为不确定性而放弃等待。

**Acceptance Criteria:**
- [ ] 处理开始后显示进度状态，包含以下阶段：语音转录中 / PPT 内容解析中 / 翻页时间轴构建中 / 笔记生成中
- [ ] 显示预估剩余时间（基于音频时长估算）
- [ ] 处理过程中用户可关闭页面，完成后通过页面内通知提醒（MVP 阶段不需要邮件通知）
- [ ] 处理失败时显示失败原因和重试入口，失败不扣减任何使用额度

---

### Story 3A：查看被动学习笔记（课后）

**As** 小林，**I want** 课后查看系统自动生成的每页 PPT 对应的结构化知识笔记，**so that** 不用回看视频就能掌握老师讲解的全部关键内容。

**Acceptance Criteria:**
- [ ] 按 PPT 页面粒度展示被动学习笔记，**所有页面均生成**，每页包含：① PPT 页面 bullet 列表 ② 每个 bullet 的 AI 行级注释 ③ 每条注释对应的引用时间戳 ④ page_supplement 补充说明（老师讲了但不属于任何 bullet 的内容）
- [ ] 点击时间戳，音频播放器跳转到对应时间点并自动播放
- [ ] 老师未讲解的 bullet：显示 bullet 原文，注释区域留空（不显示占位文字）
- [ ] 三栏布局展示：左栏可折叠大纲导航（Hover 浮出缩略图，点击跳转），中栏 PPT 主画布（展示用户图钉标记），右栏动态笔记卡片区
- [ ] 若某页 PPT 对应的讲解内容不足 10 秒，标注「讲解内容较少，建议参考上下页」
- [ ] 对齐置信度低（系统估算）的页面，在页面标题处显示 ⚠️ 标识

---

### Story 3B：查看主动学习笔记（课后，有用户笔记时）

**As** 小林，**I want** 查看 AI 基于我的课堂笔记和老师逐字稿扩写后的笔记，**so that** 我记录的关键词和疑问能被补全为完整的知识点。

**Acceptance Criteria:**
- [ ] 仅当用户有笔记输入时，在被动学习笔记**基础上叠加**展示主动学习笔记模块
- [ ] 有用户笔记的页面同时展示：被动学习 bullet 注释（完整保留） + 主动学习扩写笔记（额外叠加）
- [ ] 每条扩写笔记展示：① 用户原始输入 ② AI 扩写后的内容（基于对应逐字稿）
- [ ] 扩写内容限定在对应时间段的逐字稿范围内，不引入其他知识（防幻觉）
- [ ] 右上角「我的笔记 | AI笔记」视图切换器：「我的笔记」模式仅展示用户原始图钉骨架，「AI笔记」模式展示被动学习注释 + 主动学习扩写完整内容
- [ ] 图钉与笔记卡片通过贝塞尔曲线连接，视图切换时连线与 PPT 上的图钉/高亮同步更新

---

### Story 4：导出笔记（P1）

**As** 小林，**I want** 将生成的笔记导出为 Markdown 或 PDF，**so that** 我可以在 Notion/Obsidian 中管理知识或跨设备保存复习。

**Acceptance Criteria:**
- [ ] 提供「导出全部」选项，支持 Markdown（主推）和 PDF 两种格式
- [ ] Markdown 导出：按 PPT 页面组织层级结构，用户原始笔记以 `> 💡 **[我的课堂笔记]**` 格式高亮区分，每条注释附带时间戳引用
- [ ] PDF 导出：每页 PPT 为一个区块，包含 PPT 页面截图、bullet 行级注释（含时间戳引用文字）、page_supplement 补充说明；被动学习和主动学习笔记分区展示
- [ ] 导出文件名格式：`LiberStudy_{PPT文件名}_{日期}.md` / `.pdf`
- [ ] PDF 导出在前端使用 jsPDF + html2canvas 实现，无需后端参与
- [ ] 导出过程显示进度条，60 页以内 30 秒内完成（支持分页懒渲染）

---

### 约束与边界情况

| 类型 | 描述 |
|------|------|
| **技术约束** | MVP 阶段仅支持单一语言（中文或英文）的 ASR；不支持混合语言（如中英夹杂课堂）自动分离 |
| **文件约束** | PPT 必须与录音对应同一场讲课；不支持多 PPT 文件对应一段录音 |
| **边界情况** | 场景①：用户未手动打点时，系统通过语义匹配推断各页起止时间；置信度低时标注「⚠️ 对应时段为系统估算」。场景②：用户无有效锚点时（如未插入任何笔记），系统使用逐字稿与课件页面文本的语义相似度匹配构建时间轴，严禁直接均分 |
| **边界情况** | 用户笔记为空（仅录音不记笔记）时，只生成被动学习笔记，不触发主动学习 |
| **笔记生成策略** | **被动学习为基础层，所有页面均生成**；用户在某页主动添加了笔记（图钉/文字输入），该页**额外叠加**主动学习扩写，被动学习的 bullet 行级注释完整保留；用户可通过视图切换器选择查看模式 |
| **边界情况** | 若某页 PPT 对应的讲解内容不足 10 秒（如教师一翻而过），该页笔记标注"讲解内容较少，建议参考上下页" |
| **边界情况** | 录音中存在大段静默（>2 分钟）时，系统跳过静默段不生成笔记 |
| **使用限制** | 每用户每天最多处理 2 次，单次音频最长 120 分钟 |
| **部分失败策略** | LLM 笔记生成按页独立调用（最多重试 3 次）；部分页面失败时，系统进入 `partial_ready` 状态：成功页面正常展示笔记，失败页面显示「该页笔记生成失败，点击重试」按钮，支持单页重试；ASR 或对齐阶段整体失败则进入 `error` 状态 |
| **数据存储** | MVP 为本地运行模式，所有数据存储在用户本地（后端临时文件 + 前端 IndexedDB），不涉及服务器端数据留存 |

---

## 8. Out of Scope

### MVP 阶段明确不做

| 功能 | 排除原因 |
|------|---------|
| **实时笔记生成**（边录音边实时生成结构化笔记，V2） | 实时流式 ASR + 实时 LLM 推理架构复杂度高、延迟难以保证；MVP 阶段使用课后批量生成已足够验证核心假设 |
| **RAG 智能问答**（对笔记提问） | 属于二期功能；需先验证"生成笔记质量是否足够好"，再构建问答上层；MVP 被动学习仅提供老师逐字稿内容 |
| **自动生成复习闪卡** | 是笔记质量验证后的自然延伸，不在 MVP 范围内 |
| **移动端 App**（iOS / Android） | 目标用户主要在桌面端处理课程内容；iPad 手写笔记输入是 V2 扩展方向 |
| **多用户协作**（共享笔记、团队空间） | 职场场景的核心需求，但 MVP 聚焦个人用户价值验证 |
| **多说话人识别**（区分老师/学生发言） | 技术复杂，且 MVP 场景（单讲者课堂）不需要 |
| **直播/在线课程 URL 直接导入**（B站、YouTube） | 涉及版权和下载合规性问题；V2 阶段规划为网课录屏回放输入模式 |
| **笔记手动编辑** | AI 生成笔记的内容修改需要富文本编辑器和持久化同步，复杂度高；MVP 阶段笔记内容只读，支持保存（IndexedDB）和导出（Markdown/PDF）|

### 未来可考虑

- **个性化笔记风格**：不同 Persona（小林 vs. 小陈）偏好不同的笔记详细程度和结构
- **知识图谱可视化**：将多节课笔记中的概念关系可视化
- **Anki / Notion 直接集成**：一键导出到第三方笔记工具

---

## 9. Dependencies & Risks

### 技术依赖

| 依赖项 | 用途 | 当前状态 | 备选方案 |
|--------|------|---------|---------|
| **阿里云智能语音 API** | 中文 ASR，录音/音频文件转带时间戳逐字稿 | 需申请 API Key，有 10 小时/月免费额度 | 讯飞 ASR API（精度相近，计费模式不同） |
| **OpenAI Whisper API** | 英文 ASR，录音/音频文件转带时间戳逐字稿 | 需 API Key，$0.006/分钟 | 本地 Whisper large-v3（有 GPU 时可用） |
| **Claude API（claude-sonnet）** | 笔记生成：PPT bullet + 逐字稿 → 行级注释 + 引用时间戳 | 需 Anthropic API Key | GPT-4o（接口兼容，成本略高） |
| **OpenAI text-embedding-3-small** | 语义对齐：逐字稿切块与 PPT 页面文本的相似度匹配 | 需 OpenAI API Key（与 Whisper 共用），$0.02/1M tokens | BAAI/bge-m3（本地运行，零成本，但需 ~500MB 模型文件） |
| **LibreOffice（Headless 模式）** | 将 .ppt/.pptx 统一转换为 PDF | 需安装 LibreOffice（~300MB），以 headless 命令行方式调用 | 仅接受 .pptx + python-pptx 解析（放弃 .ppt/.pdf 支持） |
| **PyMuPDF（fitz）** | 解析 PDF 提取每页文本+布局，渲染为 PNG 缩略图/画布图 | 可直接使用（pip install pymupdf） | pdfplumber（文本提取）+ pdf2image（渲染） |
| **Web Audio API / MediaRecorder API** | 浏览器端实时录音（场景②） | 浏览器原生支持，无需额外依赖 | — |
| **FFmpeg** | 音频格式转换：WebM/Opus → WAV（ASR 输入）；处理用户上传的 MP3/M4A | 需安装 FFmpeg（各平台均有预编译包） | pydub（Python 封装，但底层仍依赖 FFmpeg） |
| **FastAPI + Uvicorn** | 后端 API 服务，处理文件上传和 AI 调用 | 可直接使用（pip install） | Flask（更轻量，异步支持弱） |
| **React + Vite** | 前端交互界面 | 可直接使用（npm） | Next.js（全栈框架，但与 Python 后端配合复杂度高） |

### 外部依赖

- **LLM API 稳定性**：Claude API 的可用性直接影响处理成功率；需实现重试机制（最多 3 次，间隔 5 秒）
- **阿里云 ASR 免费额度**：10 小时/月免费额度，超出后约 ¥5.4/节课；MVP 阶段通过每日处理次数限制控制成本
- **OpenAI Whisper API 费用**：英文课程约 ¥3.9/节，MVP 阶段按量付费，无需预付

### 风险与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|---------|
| **场景②：用户忘记或懒于记笔记（无锚点输入）** | 中 | 中（对齐精度下降，仅能依赖语义匹配） | 语义相似度匹配兜底，严禁均分；UI 引导用户至少标记几个关键页 |
| **场景①：纯音频语义对齐精度不足** | 中-高 | 中（笔记结构可能不准） | 提供可选的手动打点界面；对齐置信度低时明确标注 ⚠️ 管理预期 |
| **ASR 对专业术语识别差**（公式读法、英文缩写） | 中 | 中（笔记准确性下降） | 允许用户上传课程专业词表；LLM 融合阶段参考 PPT 文字进行校正 |
| **LLM 生成笔记产生幻觉**（编造未讲的内容） | 低-中 | 高（信任度崩溃） | Prompt 工程约束（"仅基于提供的逐字稿内容生成，不补充未出现的知识"）；上线前人工抽样审核 |
| **处理时长超出用户预期**（90分钟课 > 15分钟） | 中 | 中（用户等待流失） | 异步处理 + 离开页面后可回来查看；优化模型调用并发度 |
| **长时录音文件上传稳定性**（90分钟+ 音频文件较大） | 低-中 | 中（用户上传失败体验差） | 分片上传 + 断点续传；超大文件给出压缩建议 |
| **API 成本超预算**（用户大量处理长录音） | 低 | 高（资金压力） | MVP 阶段限制：每用户每天最多处理 2 次，单次音频最长 120 分钟 |
| **贝塞尔曲线连线前端复杂度**（SVG 层实时重绘、缩放/滚动同步、视图切换动画） | 中 | 低（不影响核心功能，影响体验精致度） | 优先实现基础连线（静态 SVG path）；若开发周期紧张，降级为图钉与笔记卡片的编号/颜色对应方案，V1.1 补全动画与淡入淡出 |

---

## 10. 场景业务逻辑与核心技术挑战

本章节逐场景说明完整的数据流、AI 处理逻辑，以及每个场景面临的核心技术难题。开发者在实现前应完整阅读本章。

---

### 核心概念：音频-PPT 对齐问题（Alignment）

LiberStudy 的本质是将两个独立的信息流对齐：

```
信息流 A：音频转录文本（带时间戳的逐字稿）
           00:00 - 02:30  "今天我们来讲第三章，微积分的基本定理..."
           02:30 - 08:15  "首先来看极限的定义，当x趋近于a时..."

信息流 B：课件页面内容（静态结构，统一按 PDF 处理）
           第1页：第三章 微积分基本定理
           第2页：3.1 极限的定义
           第3页：3.2 导数与微分
           ...
```

**目标**：为每页课件找到对应的逐字稿时间段，然后用 LLM 将"课件框架 + 老师口语讲解"融合为结构化笔记。

**对齐的准确性直接决定笔记质量**：对齐错误意味着某页课件配上了另一页的讲解内容，生成的笔记会严重失真。

---

### 场景②：课中实时录音（开发优先级最高）

**难度：中 | 对齐方式：用户笔记锚点 + 语义相似度校准**

#### 数据流

```text
举例：
上课阶段（实时）：
  初始化系统时钟：录音开始瞬间，系统生成全局 Recording_Start_Epoch，后续所有操作记录基于此换算为“相对音频时间”。
  防抖与锚点记录：用户操作课件翻页 → 停留超过设定阈值（如10秒）或发生实际输入行为（插入笔记文字）
  系统记录：{ 页码: 3, 相对时间戳: "00:23:45", 笔记: "极限定义要记" }
  麦克风持续录音 → 本地分片保存（如每10分钟切片，防丢失及应对ASR限制） → 生成音频文件列

课后阶段（批量）：
  Step 1: 音频切片并发处理 → ASR → 拼接为带统一相对时间戳的完整逐字稿
  Step 2: 文档统一处理：用户上传 PPTX 或 PDF → 系统统一转换为 PDF 格式 → 解析 PDF 提取每页结构化内容
          （针对纯图片/公式页，若PDF提取文本极少，旁路调用 OCR/VLM 补全结构）
          同时：PDF → 每页渲染为PNG截图（供UI左侧展示）
  Step 3: 构建“非线性”翻页时间轴：
          结合防抖后的有效用户锚点，构建 {页码: [时间段A, 时间段B...]} 的映射关系（支持讲师前后翻页回顾）。
          翻页时间轴 + 逐字稿 → 结合“文本语义相似度”切割每页对应的逐字稿片段。
  Step 4: 行级笔记生成与噪音过滤（每页单独调用LLM）
          每页：结构化bullet列表 + 对应逐字稿片段 → LLM
          LLM 输出：每个bullet的注释 + 引用时间戳 + [Out_of_Context_Notes]（用于收纳课件外的重要延伸，丢弃无学术价值的闲聊/噪音）
```

#### 对齐原理与容差机制

用户在哪页课件记笔记或有效停留，该时间戳作为该页的**参考锚点**，而非精确起始时间。用户翻页行为与讲师实际讲解之间存在偏差，因此这些时间戳仅供 AI 定位参考，绝不能视为硬边界。

同时，课堂讲解具备非线性特征（讲师可能随时回顾前文）。系统的数据结构必须支持**一页课件对应多个不连续的音频片段**。

**无笔记页面的处理（语义校准）**：
当用户只在部分页面记笔记时，严禁使用“按时长均分”策略。对于两个时间锚点之间的长音频段，系统需将其对应的逐字稿切块，并与这几页 PDF 的文本特征（Embedding）进行相似度匹配。PDF 文本对应密度最高的时间段，才被判定为该页真正的讲解核心区。

#### 行级笔记生成（Step 4）

场景②的行级生成以页面为单位单次调用 LLM，**所有页面均生成被动学习笔记**，有用户笔记的页面**额外叠加**主动学习扩写：

**所有页面（被动学习 — 基础层）**：
输入该页结构化 bullet + 逐字稿片段，LLM 输出每个 bullet 的行级注释 + 引用时间戳 + `page_supplement`。

**有用户笔记的页面（主动学习 — 叠加层）**：
在被动学习基础上，额外将用户原始笔记 + 对应逐字稿送入 LLM，扩写用户笔记（补全要点、纠正表述、补充老师说明）。最终展示：被动学习 bullet 注释（完整保留） + 用户原始笔记 → AI 扩写内容（叠加展示）。

> **设计理由**：用户可能只记了"重点"两个字，如果因此跳过被动学习，该页反而丢失了系统本可提供的完整讲解结构化。叠加方案确保所有页面的信息完整性，同时让用户的主动思考得到 AI 增强。

**噪音过滤与延伸处理**：
课堂逐字稿中必然包含“闲聊”、“纪律维持”等无关噪音。在 Prompt 中必须明确赋予 LLM **“丢弃无关信息的权力”**。同时，对于讲师脱离课件发挥的重要知识点，LLM 需将其归纳至额外的 `[Out_of_Context_Notes]` 字段中，防止强行将不相关的内容附着在现有的 bullet 上。

#### 核心难点与系统级应对方案

1. **绝对时间与相对时间的错位（录音中断与起始对齐）**：
   * **应对**：以 `Recording_Start_Epoch` 为基准。录音暂停时，系统累加 `Pause_Duration`。后续发生的所有操作时间戳，在与 ASR 的相对时间对齐时，必须扣除该 `Pause_Duration` 偏移量。
2. **“伪操作”导致的锚点污染**：
   * **应对**：引入前端防抖机制。连续快速翻页产生的短暂停留不计入时间轴，仅记录停留时长达标或有实质性笔记输入的页面为有效锚点。
3. **超长音频与 ASR 崩溃风险**：
   * **应对**：客户端必须实现音频的分片逻辑（Chunking），课后批量并发上传和调用 ASR 接口，避免单一大文件造成的上传超时或接口拒绝服务。
4. **非标准排版文档的解析盲区**：
   * **应对**：统一转 PDF 处理降低了格式兼容复杂度，但面对满是公式截图或图表的页面，仍需设计 OCR/VLM 的旁路补偿机制，以确保 Step 4 的 LLM 始终有结构化的基准文本可用。

---

### 场景①：课后上传录音文件（P1，尽力做）

> **优先级说明**：场景①效果受限于纯音频语义推断的准确度，对齐准确率天花板低于场景②。MVP 阶段以场景②为核心保障，场景①作为补充能力，尽力实现但不对齐精度做强承诺。

**难度：高 | 对齐方式：页级语义推断 + 行级 LLM 内部对齐**

#### 数据流

```text
用户上传：
  音频文件（MP3/WAV/M4A） + 课件文件（PPTX/PDF）

后端处理：
  Step 1: 音频 → ASR → 带相对时间戳逐字稿
  Step 2: 文档统一处理：全部转换为 PDF 格式 → 解析提取每页结构化内容
          （复用场景②的基建：对于纯图片/公式页，旁路调用 OCR/VLM 补全文本结构）
  Step 3: 页级对齐 → 为每页 PDF 找到对应的逐字稿核心时间段
  Step 4: 行级笔记生成 → 复用场景②的 LLM 调用逻辑

输出：
  每个bullet → 该bullet对应的老师讲解注释 + 对应的录音时间戳溯源（老师未讲则留空）
```

#### Step 3：页级对齐（原理性说明）

场景①没有任何时间锚点，需依靠语义匹配推断每页课件的起止时间段。核心思路：

1. **语义相似度匹配**：将每页提取的文本转换为 Embedding，在逐字稿中寻找语义最相近的片段区间
2. **顺序约束**：利用讲师大概率按课件顺序讲解的先验，对匹配结果做顺序性校验和修正
3. **低置信度降级**：匹配失败的页面，基于前后已确定页面的时间戳做局部插值估算，UI 标注 ⚠️

> **注意**：具体的 Embedding 模型选型、滑动窗口参数、相似度阈值等算法细节，需在有 3-5 组真实课程数据验证后确定，不在 PRD 中预设。

#### Step 4：行级笔记生成

复用场景②的设计：单次 LLM 调用完成对齐+生成，所有页面生成被动学习笔记。Prompt 结构与场景②相同（见上文）。

#### 用户手动辅助（可选兜底与系统纠偏）

系统在生成笔记前/后均提供**可选的时间轴打点界面**：
```text
用户在录音/笔记核对界面，可拖动进度条 → 点击”这里是第N页开始” → 手动创建硬锚点
```
一旦用户手动创建锚点，系统将其视为场景②的真实锚点，立即覆盖 AI 估算结果，并重新触发对齐与笔记生成，刷新该页及相邻页的笔记结果。

#### 场景①的设计性局限

> **根本局限**：缺乏实时行为数据，对齐准确率强依赖 ASR 质量与课件的文本密度。这是纯算法推断的客观物理限制，应在产品结果页明确告知用户管理预期。

---

### 场景②与场景①横向对比

| 维度 | 场景②（P0） | 场景①（P1） |
|------|------------|------------|
| **对齐方式** | 用户笔记防抖锚点 + 语义相似度校准 + 行级LLM | 页级语义推断 + 行级LLM |
| **对齐准确率** | 中高（锚点越多越准） | 中（取决于PPT文字量） |
| **用户操作负担** | 中（上课时需主动打锚点） | 低（上传后等待） |
| **技术实现难度** | 中 | 中高 |
| **笔记质量稳定性** | 高（有用户笔记输入时） | 低-中（对齐可能出错） |

> **MVP-0 演示策略**：场景②是最佳演示路径，笔记质量最可控、用户操作直观。场景①作为补充，演示时使用文字密集型 PPT 以保证语义匹配成功率。

> 场景③（课后上传录频文件）和场景④（实时网课监听）的技术说明已移至**附录 A**。

---

## 11. UI 规格与交互规范

本章定义前端展示规格、交互动线约定和数据结构规范。

### 11.0 页面导航与状态机

#### 页面导航图

```text
┌─────────────┐     点击”新建课堂”      ┌──────────────────┐
│  大厅工作台   │ ──────────────────────→ │  创建新课堂弹窗    │
│  (Lobby)     │                        │  选择：实时录音/    │
│              │ ←── 点击 Logo 返回 ──── │  上传文件          │
└──────┬───────┘                        └────┬────┬────────┘
       │                                     │    │
       │ 点击历史卡片                   选择场景②  选择场景①
       │                                     │    │
       │                                     ▼    ▼
       │                           ┌──────────────────────┐
       │                           │  上课采集界面（场景②） │
       │                           │  / 文件上传（场景①）   │
       │                           └──────────┬───────────┘
       │                                      │ 点击”生成笔记” / “开始处理”
       │                                      ▼
       │                           ┌──────────────────────┐
       │                           │  处理进度界面          │
       │                           │  (Processing)         │
       │                           └──────────┬───────────┘
       │                                      │ 处理完成
       │                                      ▼
       └──────────────────────────→ ┌──────────────────────┐
                                    │  课后笔记查看界面      │
                                    │  (三栏沉浸式视图)      │
                                    └──────────────────────┘
```

#### 笔记生成状态机

```text
                ┌─────────┐
                │  idle   │  ← 用户进入大厅 / 创建页面
                └────┬────┘
                     │ 用户上传文件 / 点击”生成笔记”
                     ▼
                ┌──────────┐
         ┌──────│ uploading│  ← 音频/PPT 上传中（显示百分比）
         │      └────┬─────┘
    上传失败         │ 上传完成
         │           ▼
         │      ┌─────────────┐
         │      │ transcribing│  ← ASR 转录中
         │      └────┬────────┘
         │           ▼
         │      ┌──────────┐
         │      │ aligning  │  ← 翻页时间轴构建 + 对齐中
         │      └────┬──────┘
         │           ▼
         │      ┌────────────┐
         │      │ generating │  ← LLM 笔记生成中
         │      └────┬───────┘
         │           │
         │           ├──（全部成功）──→ ┌─────────┐
         │           │                 │  ready   │  ← 笔记可查看
         │           │                 └─────────┘
         │           │
         │           └──（部分失败）──→ ┌───────────────┐
         │                             │ partial_ready  │  ← 成功页面可查看，
         │                             │                │    失败页面显示重试按钮
         │                             └───────────────┘
         │
         └──→ ┌─────────┐
              │  error   │  ← 整体失败（ASR/对齐阶段错误），显示原因 + 重试
              └─────────┘
```

---

### 11.1 全局三栏布局（Tri-pane Layout）

系统采用左、中、右三栏响应式布局，支持灵活的区域收缩与尺寸拖拽：

1. **左栏：可折叠大纲导航（Collapsible Sidebar）**
   * **形态**：类似 Edge 浏览器垂直标签页。
   * **交互**：默认收起（仅展示极小缩略图图标），鼠标悬停（Hover）时向右浮出完整宽度的幻灯片缩略图列表。点击跳转并高亮当前页。
2. **中栏：PPT 交互主画布（Main Canvas）**
   * **形态**：居中展示当前解析的 PDF/PPT 高清画面。
   * **功能**：作为核心的阅读与“触控/点击”区域，承载用户的“图钉（Pin）”标记与 AI 的“知识高亮（Highlight）”。
3. **右栏：动态笔记与批注区（Dynamic Notes Panel）**
   * **形态**：承载笔记卡片的流式容器。
   * **顶部操作区**：**右上角新增“视图切换器”（View Toggle），支持在「我的笔记」与「AI 笔记」之间一键切换。**
   * **交互调节**：中栏与右栏之间存在可拖拽的分界线（Draggable Resizer），用户可自由分配屏幕占比。

---

### 11.2 核心交互动线一：课中手动批注（“图钉”模式）

在实时录音或课后复听场景中，用户的主动记录动线如下：

* **动作一：定点（扎图钉）**
  用户鼠标点击中栏 PPT 画面的具体位置（如某个公式旁）。前端立即生成一个“图钉”图标，后台悄悄记录此刻的**录音相对时间戳**。
* **动作二：记录（侧栏联动）**
  扎下图钉的瞬间，右侧批注区自动新建一张“笔记卡片”并获取焦点。屏幕上生成一条**贝塞尔曲线（连接线）**，将 PPT 上的图钉与右侧对应的笔记卡片物理连接起来。
* **动作三：空间堆叠**
  一页 PPT 上的多个图钉，右侧笔记卡片默认严格按照图钉在 PPT 上的 **Y 轴空间位置（从上到下）** 进行物理堆叠，确保视线平行。

---

### 11.3 核心交互动线二：课后 AI 笔记增强与视图切换

录音结束后，用户可通过右上角的 Toggle 按钮自由切换笔记形态：

#### 模式 A：我的笔记（Raw Notes View）
* **展示内容**：仅展示用户上课时真实敲下的”骨架”内容（例如图钉记录的：”极限定义”、”重点考”、”期末必看”）。
* **UI 特征**：极其克制、简洁。连线仅指向用户真实扎下过图钉的位置。

#### 模式 B：AI 增强笔记（Enhanced Notes View - 默认态）
* **生成逻辑（叠加模式）**：
  * **所有页面（被动学习 — 基础层）**：AI 自动生成该页 PPT 核心要点的 bullet 行级注释并标注引用时间戳。
  * **有用户笔记的页面（主动学习 — 叠加层）**：在被动学习注释基础上，额外展示用户原始笔记的 AI 扩写内容。被动学习 bullet 注释完整保留，不会被主动学习替换。
* **UI 特征**：
  * **中栏画布**：保留用户图钉标记（与模式 A 一致），不做额外的 AI 画布高亮（MVP 简化，V1.1 可扩展基于 PDF 坐标的文本高亮）。
  * **连线**：图钉连线指向用户真实扎下过图钉的位置（与模式 A 相同）。
  * **卡片呈现**：右侧展示排版精美的结构化笔记，保留时间戳溯源。有用户笔记的页面，卡片分为上下两区：上区为被动学习 bullet 注释，下区为用户笔记 + AI 扩写。

```text
┌──────────────────────────────────────────────┐
│ 右栏笔记区头部                                 │
│                             [我的笔记 | AI笔记]│ ⬅ Granola 式切换器
├──────────────────────────────────────────────┤
│ 🔗 {与中栏图钉相连的视觉引线}                   │
│                                              │
│ [极限的定义] ✏️ (包含用户原笔记: "重点考")       │
│ • 极限的定义要求ε>0，老师强调了邻域的概念...     │
│ • 考试常考利用定义证明多项式极限               │
│                                              │
│ ⏱ 00:23:45  ▶ 点击播放老师原音                │
└──────────────────────────────────────────────┘
```

---

### 11.4 数据与状态规范（支持双态视图）

为了支撑“图钉 + 连线 + 视图切换”的 UI 逻辑，前端与后端的数据结构交互需遵循以下规范：

**1. 笔记对象（Note Object）扩展**
为支持 Granola 式的双态切换，笔记对象必须同时保存用户的原始输入和 AI 的生成结果：
* `note_id`: 唯一标识符
* `page_num`: 归属的课件页码
* `type`: `user_manual` (手动图钉，纯用户输入) | `ai_generated` (被动学习 bullet 行级注释，**所有页面均生成**) | `ai_enhanced` (有用户笔记的页面，基于用户图钉扩写，**叠加在被动学习之上**)
* `timestamp`: 对应的录音时间偏移量（用于点击播放原音）
* `position_y`: 锚点或高亮区域在 PPT 上的相对 Y 坐标（百分比 `0.0 - 1.0`）
* `raw_content`: 用户手动输入的原始文本（在「我的笔记」模式下展示，如果纯AI生成则为空）
* `enhanced_content`: AI 润色/扩写后的结构化文本（在「AI 笔记」模式下展示）

**2. LLM 输出 JSON Schema**

后端对每页 PPT 调用 LLM 时，要求返回以下 JSON 结构（通过 Prompt 约束 + JSON Mode）：

```jsonc
// 被动学习调用（所有页面）
// 输入：该页 PDF 提取的 bullet 列表 + 对应逐字稿片段
// 输出：
{
  "page_num": 3,
  "bullets": [
    {
      "bullet_text": "极限的 ε-δ 定义",          // 原始 bullet 文本
      "annotation": "老师强调邻域概念是理解极限的关键...", // AI 生成的行级注释
      "timestamp_start": "00:23:45",             // 引用逐字稿的起始时间
      "timestamp_end": "00:25:10",               // 引用逐字稿的结束时间
      "is_covered": true                          // 老师是否讲解了该 bullet
    }
  ],
  "page_supplement": "老师额外提到：这个定义在考试中常以证明题形式出现...", // 不属于任何 bullet 的讲解内容
  "out_of_context_notes": "老师提醒下周交作业",   // 无学术价值的闲聊/事务性内容（前端不展示，仅存档）
  "coverage_summary": {
    "covered_bullets": 3,                         // 老师讲解过的 bullet 数
    "total_bullets": 5,                           // 该页 bullet 总数
    "transcript_seconds": 145                     // 该页对应逐字稿总秒数
  }
}
```

```jsonc
// 主动学习调用（有用户笔记的页面，叠加在被动学习之上）
// 输入：用户原始笔记 + 对应逐字稿片段
// 输出：
{
  "page_num": 3,
  "enhanced_notes": [
    {
      "raw_input": "重点考",                       // 用户原始输入
      "expanded_content": "极限的 ε-δ 定义是期末必考内容，老师强调需要掌握利用定义证明多项式极限的方法...", // AI 扩写
      "timestamp_start": "00:23:45",
      "timestamp_end": "00:25:10"
    }
  ]
}
```

> **Prompt 工程约束**：LLM 输出必须严格基于提供的逐字稿内容，`annotation` 和 `expanded_content` 中不得引入逐字稿中未出现的知识。`is_covered: false` 的 bullet 不生成注释。

**3. 连线渲染逻辑（Canvas/SVG 层）**
* 连线层需作为一个独立且透明的 SVG/Canvas 图层。
* 当用户切换右上角的 `[我的笔记 | AI笔记]` 视图时，前端需动态过滤并在 PPT 画布上隐藏/显示对应的图钉和高亮区域，SVG 连线必须同步触发重绘动画。
* 当中栏缩放（Zoom）或右侧滚动（Scroll）时，引线的起点和终点必须实时重绘。卡片滚出可视区时，引线做淡出（Fade-out）处理。


### 引用与可追溯性规范

**设计原则**：所有 AI 输出内容必须能追溯到原始逐字稿的具体时间段。

---

### 11.6 数据持久化与导出规范

本系统的数据流转策略分为“内部工作台沉浸式复习”与“外部结构化知识流转”两条主线，以满足学生日常上课与期末归档的不同诉求。

#### 路线一：沉浸式工作台（In-App Workspace）
**目标**：提供类似 WPS 或 Office 的顺滑体验，用户在网站的历史记录中点击课程，即可 100% 还原“左侧 PPT + 右侧图钉笔记 + 录音高亮同步”的完整交互界面。

* **底层存储实现：IndexedDB**
    * 严禁使用存在 5MB 容量上限且易失效的 `localStorage`。
    * 系统前端通过 `localforage` 或 `Dexie.js` 封装 `IndexedDB`，构建本地小型数据库。
    * **存储内容**：完整缓存用户上传的录音文件（Audio Blob）、课件源文件（PDF Blob）以及包含所有图钉坐标和 AI 批注的完整 JSON 数据树。
* **交互动线**：
    * 系统新增一个极其轻量的“大厅/历史记录”页面。
    * 用户打开网站即可看到过往的课程卡片（以课程名+日期命名）。
    * 点击卡片，前端瞬间从 IndexedDB 读取资源，秒开并恢复上次的听课进度与双栏 UI 状态，无需重新消耗 AI 算力和等待对齐时间。

#### 路线二：结构化知识导出（Knowledge Export）
**目标**：基于课件框架和老师讲解生成的完整笔记，支持无缝迁移至 Notion、Obsidian 等第三方知识库，并**高亮凸显学生本人的思考与手动记录**。

**1. Markdown 导出（主推，面向 Notion/Obsidian 原住民）**
* **设计原则**：利用 Markdown 的原生语法（如引用 `>`、粗体 `**`、列表 `-`）完美映射笔记的层级结构。
* **用户笔记高亮处理**：针对数据结构中 `raw_content`（我的原始笔记）不为空的节点，在 Markdown 生成时强制加入特定的视觉标识（如醒目的前缀或引用块），与 AI 纯生成的客观笔记形成明确的视觉区隔。
* **导出格式示例**：
    ```markdown
    # The ML Process: Resampling Methods
    📅 日期：2026-04-07 | ⏱ 总时长：45:20

    ## 3.1 极限的定义
    * 老师强调：极限的定义要求ε>0，重点理解邻域的概念，考试常考利用定义证明多项式极限。⏱ `[00:24:10]`
        * 💡 **[我的课堂笔记]**：这里必考大题！记得去看课本第 45 页的例题 2。 

    ## 3.2 Bootstrap 抽样特征
    * 有放回抽样意味着同一个样本点可以被多次选中。⏱ `[00:28:15]`
    * Bootstrap sample 的大小通常与原始数据集完全一致。⏱ `[00:30:20]`
        * 💡 **[我的课堂笔记]**：和上节课的无放回抽样做对比记忆。
    ```

**2. 静态 PDF 导出（降级存档方案）**
* **目标**：满足传统打印、微信群文件分享或平板手写批注的需求。
* **导出实现**：前端通过代码直接将上述 Markdown 结构渲染为 PDF，或者利用 `jsPDF` + `html2canvas` 提取右侧笔记区域生成。
* **排版特征**：采用清晰的”大纲式排版”，同样使用异色高亮框或特定图标来凸显”💡 我的课堂笔记”部分，确保用户哪怕在纸质打印件上，也能一眼看到自己上课时抓取的重点。
* **提示**：在点击导出 PDF 时，UI 需轻提示”PDF 版本将丢失录音点击回放及 PPT 联动高亮功能，建议保留本站历史记录以获得最佳复习体验。”

---

### 11.7 大厅工作台（Lobby Workspace）UI 规格

大厅是用户进入 LiberStudy 后的首屏界面，参考 NotebookLM 的极简网格布局（Grid Layout），采用”内容优先”设计哲学——无重度左侧边栏，无冗余仪表盘，直奔主题：**新建课堂**与**回到历史学习心流**。

#### 全局布局

```text
┌─────────────────────────────────────────────────────────────────┐
│ 📚 LiberStudy               🔍 筛选课程名称...                   ⚙️ 🧑 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   近期课堂 (Recent Sessions)                                      │
│                                                                 │
│   ┌───────────────┐  ┌───────────────┐  ┌───────────────┐     │
│   │               │  │ 📄 (封面缩略图) │  │ 📄 (封面缩略图) │     │
│   │      ➕       │  │               │  │               │     │
│   │   新建课堂    │  ├───────────────┤  ├───────────────┤     │
│   │ (拖拽文件至此)│  │ Chapter 3:    │  │ RNN & LSTM    │     │
│   │               │  │ Resampling    │  │               │     │
│   │               │  │ ⏱ 45:20      │  │ ⏱ 85:10      │     │
│   │               │  │ 📌 12条笔记    │  │ 📌 5条笔记     │     │
│   └───────────────┘  └───────────────┘  └───────────────┘     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### 核心模块交互说明

**顶部导航区（Top Header）**
* **课程名称筛选框**：居中的筛选输入框，按课程名称实时过滤课程卡片（MVP 仅支持名称筛选；V1.1 扩展为全文检索笔记内容）。
* **右上角**：设置入口（⚙️）与用户头像/离线状态指示器。

**新建动作卡片（Hero Action Card）**
* **形态**：网格视图永远的第一个元素（常驻左上角），虚线边框或强调色底色，与普通课程卡片区分。
* **点击**：唤起全屏”创建新课堂”弹窗/抽屉。
* **拖拽响应**：整个大厅支持文件拖拽（Drag & Drop）。用户将 PDF 和 MP3 一起拖入浏览器窗口时，大厅背景变暗，出现”松手即刻创建并解析”的视觉反馈。

**课程历史卡片（Session Card）**
* **视觉封面**：提取 PDF 第一页作为预览封面（比纯文字标题具有更强的视觉记忆唤醒能力）。
* **标题**：默认读取 PDF 文件名（去除后缀），支持双击或点击小铅笔图标快速重命名。
* **元信息**：录音总时长（`⏱ 45:20`）、笔记数量（`📌 12 条批注`）、最后访问时间。
* **快捷操作**：鼠标悬停时，卡片右上角浮现 `[⋮]` 菜单，提供重命名、导出（Markdown/PDF）、删除。

#### 卡片状态与生命周期流转

| 状态 | 触发条件 | UI 表现 |
|------|---------|---------|
| **解析中（Processing）** | 用户上传音频与 PPT 后 | 卡片置灰 + 动态扫光（Skeleton Loading），中心显示进度文本（”正在提取语音转录稿 45%...”），支持离开页面后台继续（Web Worker / 后端状态拉取） |
| **已完成（Ready）** | AI 对齐与笔记生成完成 | 完整封面与数据展示；点击卡片瞬间跳转至沉浸式批注视图，自动定位到上次退出的页码 |
| **异常/失败（Error）** | 格式不支持、PDF 加密、断网等 | 卡片标红，显示 ⚠️ 及简明错误信息，提供一键重试或删除按钮 |

#### 空状态设计（Empty State）

当 IndexedDB 中无任何课程数据时，网格区域替换为居中全屏空状态：

* **视觉**：居中大插画（体现声画对齐概念）+ 醒目主按钮”上传第一节课”。
* **文案**：
    * 主标题：”你的 AI 沉浸式课堂工作台”
    * 副标题：”上传一份课件与录音，LiberStudy 会自动为你对齐讲解、生成批注，或者试试拖拽文件到这里。”
* **Demo 注入（P1）**：提供次级操作”没有文件？**体验示例课程**”，点击后写入一份预置演示数据（约 2 分钟），帮助用户最快体验到产品的 Aha Moment。此功能为 P1，MVP-0 可先不实现。

## 12. Open Questions

### 待决策（优先级排序）

| 问题 | 背景 | 决策建议 | 决策截止 |
|------|------|---------|---------|
| **场景①语义对齐的具体算法选型** | 语义匹配的 Embedding 模型、滑动窗口参数等需要真实数据验证 | 先用 3-5 组真实课程数据跑通 baseline，再固化算法方案 | 场景①开发时 |
| **场景①时间轴打点界面是否为必选流程** | 强制用户打点可提升质量但增加摩擦；完全可选则对齐质量不稳定 | 建议：完全可选，但在处理完成页显著提示”添加时间锚点可提升笔记准确性” | Story 1B 开发时 |
| **中英混合课堂如何处理？** | 985 高校部分专业课为全英文或中英夹杂 | MVP 阶段要求用户选择主语言；混合语言支持列为后续迭代项 | Story 1A/1B 开发时 |

### 已决策（记录）

| 问题 | 决策结论 |
|------|---------|
| **是否支持纯音频输入（无视频）？** | 支持，这是场景①②的核心输入方式 |
| **被动学习与主动学习的关系？** | **叠加模式**：被动学习为基础层，所有页面均生成；主动学习为叠加层，有用户笔记的页面额外生成，不替换被动学习内容 |
| **场景③④的 PPT 翻页检测算法** | 使用感知哈希（pHash）优先，准确率 <85% 时切换至 SSIM，并提供手动校正界面（V2 实现） |
| **被动学习笔记的生成时机** | 课后批量生成，不做课中实时生成（实时生成是 V2 功能） |

---

## 附录 A：V2 场景规划（暂不启动）

以下场景在 MVP-0 阶段不启动，待场景①②验证核心假设后再开发。

### Story 1C：课后上传录频文件（场景③）

**As** 小林，**I want** 上传课堂录屏或录频文件，**so that** 系统自动提取音轨并识别 PPT 翻页时间点，无需我手动标记。

**Acceptance Criteria:**
- [ ] 支持上传 `.mp4` / `.webm` 视频文件，大小限制 ≤ 2GB
- [ ] 系统自动从视频文件中提取音轨
- [ ] 支持用户上传对应 PPT/PPTX，或由系统尝试从视频画面中提取 PPT 内容
- [ ] 系统通过帧分析（pHash/SSIM）自动检测 PPT 翻页时间点
- [ ] 展示自动识别结果的翻页时间轴，供用户手动校正（拖动时间点、合并/拆分片段）
- [ ] 翻页检测准确率低于 85% 时，系统提示用户进行手动校正

### Story 1D：实时网课监听（场景④）

**As** 小林，**I want** 在上网课时让系统实时采集系统音频和屏幕画面，**so that** 课后能自动生成结构化笔记，无需手动录制视频。

**Acceptance Criteria:**
- [ ] 支持实时采集系统音频（需用户授权）
- [ ] 支持实时采集屏幕或指定窗口的 PPT 画面（或用户手动上传 PPT 作为替代）
- [ ] 实时帧对比检测 PPT 翻页，自动构建翻页时间轴
- [ ] 课程结束后点击”停止并生成笔记”触发处理流程
- [ ] 提供翻页时间轴手动校正界面（与场景③相同）
- [ ] 系统音频采集在不同操作系统/浏览器下出现权限问题时，提示用户替代方案

### 场景③技术说明

**难度：高 | 对齐方式：视频帧自动识别翻页**

```
用户上传：视频文件（MP4/WebM） + PPT文件（PPTX）

后端处理：
  视频 → FFmpeg提取音轨 → ASR → 逐字稿
  视频 → 按固定间隔采样帧（如每0.5秒一帧）
  相邻帧 → pHash感知哈希比较 → 哈希差异超阈值 → 判定翻页
  翻页时间点列表 → 翻页时间轴

笔记生成（与场景①②相同）
```

核心难点：
1. **动画过渡误判**：翻页后设置冷却期（连续帧差异持续>0.5秒才确认）
2. **激光笔/鼠标遮挡**：区域过滤（忽略小范围局部变化，仅关注全局结构变化）
3. **视频质量差**：pHash准确率<85%时自动切换至SSIM或CLIP特征相似度

### 场景④技术说明

**难度：最高 | 对齐方式：实时帧识别翻页**

额外挑战：
1. **系统音频采集**：浏览器无法直接采集系统音频，需用户共享屏幕时勾选”共享系统音频”
2. **流式 ASR**：需要流式语音识别接口
3. **实时帧分析延迟**：采样、哈希计算、翻页判断需在200ms内完成
4. **DRM保护网课**：部分平台对屏幕录制有保护
